# Ignora todas as pastas node_modules (front-end e back-end)
node_modules/

# Ignora arquivos de log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# (Opcional, mas recomendado para builds do React)
/front-end/build

# (Opcional, mas recomendado para builds do Typescript)
/back-end/dist